# 猹猹街并行开发状态

更新时间：2026-08-10

| 工作流 | 状态 | 当前交付 | 阻塞 |
|---|---|---|---|
| 总控集成 | 已完成本地候选版 | 冻结 V1 钱包、XP、三片九位瓜田契约，完成生产构建与 3107 试玩验收 | 等待真实服务与真机验收 |
| 游戏循环 | 已完成 V1 规则 | 每日五籽合一、12 小时成长、九瓜收获与 XP | 无 |
| 数据安全 | 已完成本地实现 | V1 钱包/经济流水、播种收获事务、旧 seed_count 迁移、原有审核封禁 | 未连接真实 Supabase；未选真实身份绑定提供商 |
| 趣味界面 | 已完成 V1 实现 | 空藤昼夜底图、三片大热区、九株 DOM 精灵、播种/收获反馈 | 未做三类系统真机测试 |
| 地理发现 | 已完成本地实现 | 25 个公共地点、安全寻瓜状态、定位误差区间与敏感地点防护 | 5 个地点上线前仍需线下复核 |
| 质量验收 | 已完成自动化验收 | 375/430/桌面视口、社区交互、隐私脱敏与能力降级场景 | 真机、NVDA 与十连跑待补 |

## 当前已验证

- TypeScript：通过。
- ESLint（`src`、`tests`）：通过。
- Next.js 16.3 生产构建：通过。
- V1 领域规则：7/7 通过；V1 migration 事务契约：8/8 通过。
- 安全与服务端单测：26/26 通过。
- V1 领域/API/迁移/权限静态验收：28/28 通过。
- V1 三视口瓜田闭环 E2E：24/24 通过。
- 全套 Playwright：132 通过、8 项按项目配置跳过；唯一一次桌面夜间并发时序红灯单独复跑 1/1 通过。
- Next.js 16.3 production build：通过；本地 production 预览运行于 `http://127.0.0.1:3107/`。
- 地理寻瓜纯函数：20/20 通过（任务内定向执行）。
- 390px 应用内浏览器：无横向溢出、无 console error；五一广场 12 颗瓜、雷达 5 颗代表瓜、远程无评论输入框。
- Zone/community E2E：14/14 通过。
- Core E2E：24/24 通过（375px、430px、桌面）。
- Accessibility/privacy E2E：三项目等效 24/24 通过，敏感地点在瓜区、读瓜与埋瓜全链路脱敏。

## 仍需真实环境验证

- 在预发布 Supabase 执行迁移并验证 RLS、并发留籽、封禁/解封与审核流程。
- 配置 `SUPABASE_SECRET_KEY`（新 `sb_secret`，仅作 `apikey`）、`CHACHA_READ_TOKEN_SECRET`、`CHACHA_PRESENCE_TOKEN_SECRET`。
- iOS Safari、Android Chrome、鸿蒙系统浏览器的真实定位权限、软键盘与触感反馈。
- 选择并接入真实短信/OAuth 身份提供商后，才能保证清理浏览器数据也无法绕过封禁。

## 故障隔离

任务恢复遵循 `docs/coordination/RECOVERY.md`。单个任务卡死、浏览器崩溃或测试服务退出不会回滚其他任务；总控以小提交、冻结契约和可重启的本地预览完成兜底。

## 回报格式

```text
状态 / 当前提交 / 已验证 / 契约变化 / 阻塞 / 下一步
```
