# 游戏领域模型

此目录只放置无浏览器、网络和数据库副作用的游戏规则。所有会影响结果的当前时间都必须由调用方显式传入，以保证同一输入得到同一输出。

- `lifecycle.ts`：孵化、成熟、归档与扩散范围。
- `reads.ts`：有效阅读与瓜籽奖励。
- `field.ts`：瓜田成长阶段。
- `city-opening.ts`：五城开岛门槛与时间。
- `discovery.ts`：发现评分与稳定排序。
